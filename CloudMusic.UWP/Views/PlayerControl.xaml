<UserControl
    x:Class="CloudMusic.UWP.Views.PlayerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CloudMusic.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="PlayerMainGrid" Height="60">
        <MediaElement x:Name="PlayerElement"  
                          Height="auto"  Source="{x:Bind _vm.ActiveTrack.SourceData, Mode=OneWay}" 
                          AutoPlay="True" AreTransportControlsEnabled="True"/>
        <!--<MediaElement x:Name="PlayerElement" AutoPlay="True" Source="{x:Bind _vm.ActiveTrack.SourceData, Mode=OneWay}" AreTransportControlsEnabled="False"/>
        <StackPanel Orientation="Horizontal" Height="60" HorizontalAlignment="Stretch">
            <Image Source =  "{x:Bind _vm.ActiveTrack.TrackImage.AbsoluteUri, Mode=OneWay}" Width="48" Height="48" VerticalAlignment="Center"/>
            <Slider Orientation="Horizontal" HorizontalAlignment="Stretch"/>
        </StackPanel>-->


    </Grid>
</UserControl>

<!--<MediaElement x:Name="PlayerElement"  
                          Height="auto"  Source="{x:Bind _vm.ActiveTrack.SourceData, Mode=OneWay}" 
                          AutoPlay="True" AreTransportControlsEnabled="True"
                          />-->
